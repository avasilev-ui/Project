# Technology stack

**Status:** not chosen yet.

When decided, document here:

- Framework (e.g. Vite + React or Next.js)
- Local persistence (`localStorage` / IndexedDB)
- PWA setup
- How to run dev server and build

Do not invent stack choices in code until this file is updated.
